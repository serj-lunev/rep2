What is it:
Server returns to Client capitalized sentence.


To compile Server:
$ javac Server.java

To run Server:
$ java Server

To compile Client:
$ javac Client.java

To run Client:
$ java Client

To stop Server or Client
press [Ctrl] + [C]
